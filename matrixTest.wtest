import matrix.*

describe "Neo " {

  test "es el Elegido" {
    assert.that(neo.esElElegido())
  }
  
  test "su vitalidad inicial"{
    assert.equals(10, neo.vitalidad())
  }

  test "su vitalidad después de saltar"{
    neo.saltar()
    assert.equals(5, neo.vitalidad())
  }

}